---
import type { Person } from '../../data/people';
import { groupTagsByCategory, getLinkedTagIds } from '../../data/tags';
import Tag from '../common/Tag.astro';

interface Props {
	person: Person;
}

const { person } = Astro.props;
const linkedTagIds = getLinkedTagIds();
const tagGroups = groupTagsByCategory(person.tagIds);
---

<div class="person-card" style={`--person-color: var(--${person.id}-color)`}>
	<div class="skill-groups">
		{
			tagGroups.map((group) => (
				<div class="skill-group">
					<h4>{group.category}</h4>
					<div class="tag-row">
						{group.tags.map((tag) => (
							<Tag label={tag.label} linked={linkedTagIds.has(tag.id)} tagId={tag.id} />
						))}
					</div>
				</div>
			))
		}
	</div>
	<p class="stat-line">In the games industry since {person.since}</p>
	<p class="edu-line">{person.education}</p>
	{
		person.hobbies && person.hobbies.length > 0 && (
			<div class="hobbies">
				<h4>Hobbies</h4>
				<ul>
					{person.hobbies.map((hobby) => (
						<li>{hobby}</li>
					))}
				</ul>
			</div>
		)
	}
</div>

<style>
	/* Sits inside the bar card (see ScrollyRegion.astro's .side-panel), which
	   supplies the background, border and hover glow — so this is just the
	   inner padding for the skills/hobbies content. */
	.person-card {
		background: transparent;
		padding: 0.35rem 1rem 0.75rem;
		text-align: left;
	}

	.skill-groups {
		display: flex;
		flex-direction: column;
		gap: var(--gap-sm);
		margin-bottom: var(--gap-md);
	}

	.skill-group h4 {
		margin: 0 0 0.4rem;
		font-size: var(--text-xs);
		text-transform: uppercase;
		letter-spacing: 0.1em;
		color: var(--fg-muted);
	}

	.tag-row {
		display: flex;
		flex-wrap: wrap;
		gap: var(--gap-xs);
	}

	.stat-line {
		font-size: var(--text-ui);
		color: var(--fg);
		margin: 0 0 0.25rem;
	}

	.edu-line {
		font-size: var(--text-sm);
		color: var(--fg-muted);
		margin: 0;
	}

	.hobbies {
		margin-top: var(--gap-md);
		padding-top: var(--gap-md);
		border-top: 1px solid var(--border);
	}

	.hobbies h4 {
		margin: 0 0 0.6rem;
		font-size: var(--text-xs);
		text-transform: uppercase;
		letter-spacing: 0.1em;
		color: var(--fg-muted);
	}

	.hobbies ul {
		margin: 0;
		padding: 0;
		list-style: none;
		display: flex;
		flex-direction: column;
		gap: var(--gap-xs);
	}

	.hobbies li {
		font-size: var(--text-ui);
		line-height: var(--leading-snug);
		color: var(--fg);
		padding-left: 1rem;
		position: relative;
	}

	.hobbies li::before {
		content: '';
		position: absolute;
		left: 0;
		top: 0.5em;
		width: 5px;
		height: 5px;
		border-radius: 50%;
		background: var(--person-color);
	}

	@media (max-width: 900px) {
		.person-card {
			padding: 0.25rem 0.65rem 0.5rem;
		}
	}
</style>
